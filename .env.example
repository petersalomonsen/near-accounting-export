# API Server Configuration
PORT=3000
DATA_DIR=./data

# CORS Configuration
# Comma-separated list of allowed origins for cross-origin requests
# Use '*' to allow all origins (not recommended for production)
# Example: CORS_ALLOWED_ORIGINS=https://app.example.com,https://dashboard.example.com
# If not set, defaults to '*' (allow all origins)
CORS_ALLOWED_ORIGINS=*

# NEAR RPC Configuration
NEAR_RPC_ENDPOINT=https://archival-rpc.mainnet.fastnear.com
# FASTNEAR_API_KEY=your_api_key_here
# NEARBLOCKS_API_KEY=your_api_key_here
# INTENTS_EXPLORER_API_KEY=your_api_key_here
RPC_DELAY_MS=50

# Account Registration Payment Configuration
# Amount in FT base units (default: 100000 = 0.1 ARIZ with 6 decimals)
REGISTRATION_FEE_AMOUNT=100000
# Recipient account for registration payments
REGISTRATION_FEE_RECIPIENT=arizcredits.near
# FT contract ID (default: ARIZ)
REGISTRATION_FEE_TOKEN=arizcredits.near
# Maximum transaction age in milliseconds (default: 30 days)
REGISTRATION_TX_MAX_AGE_MS=2592000000
